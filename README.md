# vueAlert

> mayako

## Build Setup

``` bash
# install dependencies
npm install may-vue-alert -s

# Usage
Vue.use(VueAlert)

# Function
this.vueAlert.show('msg','type','time')

# Parameter

msg:content,
type:success|error,
time:2000|custom
